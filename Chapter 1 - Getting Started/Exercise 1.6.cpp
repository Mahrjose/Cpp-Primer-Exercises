/*

The given program fragment is illigal and give this error,

[error] expected primary-expression before ‘<<’ token

To fix this, we can remove some ";" and make the entire thing in a single line,

std::cout << "The product of " << v1 << " and " << v2 << " is " << v1 * v2 << std::endl;

*/